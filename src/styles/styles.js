// styles.js

const fontFamily = {
    fontFamily: 'Manrope-Regular',
};

class commonStyles {
    static legend01Bold = {
        ...fontFamily,
        fontSize: 50,
        fontWeight: 'bold',
    };

    static legend01Semibold = {
        ...fontFamily,
        fontSize: 50,
        fontWeight: '400', // Semibold
    };
    static heading01Bold = {
        ...fontFamily,
        fontSize: 30,
        fontWeight: 'bold',
    };

    static heading01Semibold = {
        ...fontFamily,
        fontSize: 30,
        fontWeight: '600', // Semibold
    };

    static heading01Medium = {
        ...fontFamily,
        fontSize: 30,
        fontWeight: '500', // Medium
    };

    static heading01Regular = {
        ...fontFamily,
        fontSize: 30,
        fontWeight: 'normal',
    };

    static heading02Bold = {
        ...fontFamily,
        fontSize: 26,
        fontWeight: 'bold',
    };

    static heading02Semibold = {
        ...fontFamily,
        fontSize: 26,
        fontWeight: '600', // Semibold
    };

    static heading02Medium = {
        ...fontFamily,
        fontSize: 26,
        fontWeight: '500', // Medium
    };

    static heading02Regular = {
        ...fontFamily,
        fontSize: 26,
        fontWeight: 'normal',
    };

    static heading03Bold = {
        ...fontFamily,
        fontSize: 20,
        fontWeight: 'bold',
    };

    static heading03Semibold = {
        ...fontFamily,
        fontSize: 20,
        fontWeight: '600', // Semibold
    };

    static heading03Medium = {
        ...fontFamily,
        fontSize: 20,
        fontWeight: '500', // Medium
    };

    static heading03Regular = {
        ...fontFamily,
        fontSize: 20,
        fontWeight: 'normal',
    };

    static heading04Bold = {
        ...fontFamily,
        fontSize: 18,
        fontWeight: 'bold',
    };

    static heading04Semibold = {
        ...fontFamily,
        fontSize: 18,
        fontWeight: '600', // Semibold
    };

    static heading04Medium = {
        ...fontFamily,
        fontSize: 18,
        fontWeight: '500', // Medium
    };

    static heading04Regular = {
        ...fontFamily,
        fontSize: 18,
        fontWeight: 'normal',
    };

    static body01Bold = {
        ...fontFamily,
        fontSize: 16,
        fontWeight: '700', // Semibold
    };

    static body01Semibold = {
        ...fontFamily,
        fontSize: 16,
        fontWeight: '600', // Semibold
    };

    static body01Medium = {
        ...fontFamily,
        fontSize: 16,
        fontWeight: '500', // Medium
    };

    static body02Regular = {
        ...fontFamily,
        fontSize: 16,
        fontWeight: 'normal',
    };

    static body02Semibold = {
        ...fontFamily,
        fontSize: 14,
        fontWeight: '700', // Semibold
    };

    static body02Medium = {
        ...fontFamily,
        fontSize: 14,
        fontWeight: '500', // Medium
    };

    static body02Regular = {
        ...fontFamily,
        fontSize: 14,
        fontWeight: 'normal',
    };

    static labelMedium = {
        ...fontFamily,
        fontSize: 12,
        fontWeight: '500', // Medium
    };

    static labelRegular = {
        ...fontFamily,
        fontSize: 12,
        fontWeight: 'normal',
    };

    static button1 = {
        ...fontFamily,
        fontSize: 14,
    };

    static button2 = {
        ...fontFamily,
        fontSize: 12,
    };

    static headerLegend = {
        ...fontFamily,
        fontSize: 22,
    }
    static drodownLegend = {
        ...fontFamily,
        fontSize: 11,
        fontFamily: 'Manrope-ExtraBold',
        color: "#F8F9FB50"
    }


}

export default commonStyles;
